import { CreateAppointmentPipe } from './create-appointment.pipe';

describe('CreateAppointmentPipe', () => {
  it('create an instance', () => {
    const pipe = new CreateAppointmentPipe();
    expect(pipe).toBeTruthy();
  });
});
